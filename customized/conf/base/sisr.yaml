defaults:
  - runtime
  - datasets_node02

task: "sisr"
scale: [1]
best_prec1: 10000
best_prec5: 10000
metrics: {"train": "loss", "test": "PSNR"}
save_fmt: "png"  # options: mat, not mat
test: "reduce"  # options: reduce, full experiment
test_samplers_per_gpu: 20